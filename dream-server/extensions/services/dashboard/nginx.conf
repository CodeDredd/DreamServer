server {
    listen 3001;
    listen [::]:3001;  # IPv6 support for Docker healthcheck
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # SPA routing - serve index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to status backend
    # Auth: nginx injects Bearer token for same-origin requests (B2 fix)
    # Token read from environment via envsubst in entrypoint
    location /api/ {
        proxy_pass http://dashboard-api:3002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "close";
        # B1 fix: Auth header injected by entrypoint.sh after reading key from file/env
        proxy_set_header Authorization "Bearer ${DASHBOARD_API_KEY}";
        proxy_read_timeout 180s;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    # script-src hashes:
    #   sha256-LjAdhA9OP1QNKxlLKlLPNIif1VZEIgS4Ceym55X/Je0= → theme-flash
    #     prevention IIFE in index.html (sets data-theme before React mounts).
    #   sha256-92cza1Wj/+GAS81NsKSnNMZcNV7jv2oWuTnJRsPVzmc= → service-worker
    #     registration block in index.html.
    # If either inline script body in index.html is edited, the matching
    # hash must be regenerated (echo -n "<script-body>" | openssl dgst -sha256 -binary | base64).
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'sha256-LjAdhA9OP1QNKxlLKlLPNIif1VZEIgS4Ceym55X/Je0=' 'sha256-92cza1Wj/+GAS81NsKSnNMZcNV7jv2oWuTnJRsPVzmc=' https://unpkg.com https://cdn.jsdelivr.net; style-src 'self' https://cdn.jsdelivr.net 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; font-src 'self' https://cdn.jsdelivr.net;" always;
}
