# Lokale Dev-Einstellungen — kopieren als `.env`.
# Container-Stand liest die Werte direkt aus der Container-Env (siehe
# `compose.yaml.disabled`).

DASHBOARD_API_KEY=
NUXT_API_BASE_INTERNAL=http://127.0.0.1:3002
NITRO_PORT=3011
NITRO_HOST=0.0.0.0

